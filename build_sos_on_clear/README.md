Docker container to build sos\_acrn on Clear Linux.

# Build

`make build` will create a Docker container called `sos`.

# Running

`make run` will run the Docker container and place the built images inside
the `artifacts` directory.
